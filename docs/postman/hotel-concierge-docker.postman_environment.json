{
  "id": "hotel-concierge-docker-env",
  "name": "Hotel Concierge — Docker",
  "values": [
    {
      "key": "conciergeUrl",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "ms-hotel-concierge-ai — orquestrador IA"
    },
    {
      "key": "hotelInfoUrl",
      "value": "http://localhost:8081",
      "type": "default",
      "enabled": true,
      "description": "ms-hotel-info — dados do hotel"
    },
    {
      "key": "aiDataUrl",
      "value": "http://localhost:8082",
      "type": "default",
      "enabled": true,
      "description": "ms-ai-data — RAG + cache + sessões"
    },
    {
      "key": "ollamaUrl",
      "value": "http://localhost:11434",
      "type": "default",
      "enabled": true,
      "description": "Ollama nativo Windows — não está em Docker"
    },
    {
      "key": "redisInsightUrl",
      "value": "http://localhost:8001",
      "type": "default",
      "enabled": true,
      "description": "RedisInsight UI — inspecionar vector store e cache"
    }
  ],
  "_postman_variable_scope": "environment",
  "_postman_comment": "Modo: todos os MSes + infra em Docker (docker compose up -d). Ollama corre sempre nativamente. As portas são expostas ao host, logo localhost:PORT funciona do Postman em qualquer modo."
}
